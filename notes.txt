#terminal
(cmd t to open a new terminal tab)
npm start (to start server front end)

# to stop server:  ctrl + c


# initialize the repository
git init

# add the remote url
git remote add origin <URL>


# create a local copy
git add .
git commit -m "MESSAGE"


# Push /upload
    # first push:
    git push -u origin master

# next time
    git push

